---
name: testcase-generation
description: 基于需求与详细设计生成测试用例文档，确保功能范围、文案、表述与上游文档严格一致；内置上游映射校验、文案规范化、术语转换对照与多角色可读性评审四项机制。
allowed-tools: Read Glob Grep Edit Write AskUserQuestion
---

# testcase-generation

## 1. 目标

把 `01-需求文档.md` 与 `02-详细设计文档.md` 转成 `03-测试用例文档.md`，并确保：

1. **功能范围一致** — 测试用例严格覆盖上游文档定义的功能点，不凭空增加上游未提及的功能（如按钮置灰、隐藏交互等须在 01/02 中有定义）。
2. **文案精确匹配** — 测试用例中的提示文案、校验消息、成功/失败 Toast 与 `02-详细设计文档.md` 严格逐字一致。
3. **表述专业且可读** — 面向多角色（产品、开发、测试、项目开发管理人），用通用业务术语描述状态与操作，技术枚举值在备注或括号中注明。

## 2. 适用输入

| 输入                 | 必需性 | 用途                                   |
| -------------------- | ------ | -------------------------------------- |
| `01-需求文档.md`     | 必需   | REQ 编号、功能范围、验收口径           |
| `02-详细设计文档.md` | 必需   | 接口路径、校验文案、状态映射、异常处理、**前端路由与菜单路径（第 2.3 节）** |
| `02b-后台文档.md`    | 必需   | 接口参数、错误码、retInfo 原文         |
| `00-全局说明.md`     | 可选   | 全局约束（不重复测试）                 |
| `01c-原型文档.md`    | 如有   | 交互细节参考                           |
| 模块 `TRACE.yaml`    | 如有   | 追踪矩阵基线                           |

**输入优先级**：`01` 定义功能范围 → `02` 定义实现细节与文案 → `02b` 定义接口契约。三者冲突时，按 `detail-design-bootstrap` 的真源裁决策略处理（需求优先 → 代码 → 补充）。

## 3. 期望输出

`03-测试用例文档.md`，包含：

1. 测试范围与不测项（对齐 01 边界）
2. 测试环境与数据准备（**须含前端 URL、前端路由、前端入口（菜单路径）、后端 URL、测试账号矩阵、数据初始化要求**；路由/菜单取自 `02` 第 2.3 节）
3. 功能用例 `FT-*`（含操作步骤、预期结果、文案精确匹配）
4. 接口用例 `IT-*`（含请求参数、正常/异常返回、边界值）
5. 权限用例 `PT-*`（含"直接路由访问"校验，路由取自第 2.1 环境信息）
6. 异常分支用例 `ET-*`（**分散归入功能/接口用例表格，不再单独立章**；编号带功能域粒度 `ET-{MODULE}-{FEATURE}-{NNN}`，与所在 FT 表格域前缀一致，如 `ET-BILLCFG-LIST-001`；跨域异常用 `ET-{MODULE}-X-{NNN}`）
7. 回归测试清单
8. 需求与测试用例追踪矩阵
9. 一致性检查清单

## 4. 使用步骤

1. 读取 `01` 抽取 REQ 清单 + 每个 REQ 的功能点与验收口径。
2. 读取 `02` 抽取每个 REQ 的：接口清单、校验文案（逐字）、状态映射表、异常处理表；并读取 `02` 第 2.3 节的前端路由（如 `/config/billingConfig`）与菜单路径（如 `配置中心 / 商业化计费配置`），用于 03 第 2.1 环境信息（前端路由 / 前端入口）与权限用例（直接路由访问校验）。
3. 读取 `02b` 抽取错误码与 retInfo 原文。
4. 将用户路径类场景拆成 `FT-*`，每个 FT 标记对应 REQ。
5. 将接口规则、边界、权限、状态流场景拆成 `IT-*`。
6. 对每个 FT/IT 执行 §5 上游映射校验。
7. 对每个 FT/IT 的预期文案执行 §6 文案规范化。
8. 对每个 FT/IT 的状态/枚举值执行 §7 术语转换。
9. 对全文执行 §8 多角色可读性评审。
10. 输出可直接落入 `03-测试用例文档.md` 的结构化内容 + 一致性检查清单。

## 5. 上游文档与测试用例内容映射校验规则

### 5.1 功能范围校验

每个测试用例必须能追溯到上游文档的具体功能点：

| 校验项        | 规则                                                              | 不通过时处理          |
| ------------- | ----------------------------------------------------------------- | --------------------- |
| REQ 归属      | 每个 FT/IT 必须关联一个 01 中存在的 REQ 编号                      | 删除该用例或补 01     |
| 功能点边界    | FT 测试的操作/预期必须落在 01 或 02 定义的功能范围内              | 删除超范围部分        |
| 按钮置灰/隐藏 | 仅当 02 明确描述了置灰/隐藏条件时才可测试；禁止凭"应该置灰"的推断 | 标注"02 未定义"并删除 |
| 接口覆盖      | IT 测试的接口必须在 02 第 5 章接口清单中                          | 删除或补 02           |
| 异常分支      | ET 测试的异常必须在 02 异常处理表或 02b 错误码中                  | 删除或补 02/02b       |

### 5.2 校验输出

生成测试用例后，输出一份《映射校验报告》附在 03 文档附录，格式：

```markdown
## 附录 C. 上游映射校验报告

| 用例编号 | 关联 REQ | 功能点来源(01行号) | 文案来源(02行号) | 接口来源(02b行号) | 校验结果                           |
| -------- | -------- | ------------------ | ---------------- | ----------------- | ---------------------------------- |
| FT-...   | REQ-...  | 01:L102            | 02:L411          | -                 | ✅ 通过                            |
| FT-...   | REQ-...  | 01:L237            | -                | -                 | ⚠️ 02 未定义文案，已按 01 口径编写 |
```

### 5.3 常见越界模式（禁止）

- **推断式置灰**：02 未描述某按钮置灰条件，但测试用例写"XX 按钮应置灰"。
- **臆测式校验**：02 未定义某字段校验规则，但测试用例编造校验提示文案。
- **超范围接口**：01/02 未纳入某接口，但 IT 用例测试了该接口。
- **不存在状态**：02 状态映射表中无某状态值，但测试用例使用了该状态。

### 5.4 环境信息与路由一致性校验

03 第 2.1 环境信息的路由/菜单必须与 `02` 第 2.3 节逐字一致，否则 UI 自动化按错误路由访问会导致整页失败（如停留在登录页），与脚本元素定位无关：

| 校验项 | 规则 | 不通过时处理 |
| --- | --- | --- |
| 前端路由来源 | 03「前端路由」必须等于 `02` 第 2.3 节的路由值 | 修正 03 路由 |
| 菜单路径来源 | 03「前端入口」菜单路径须等于 `02` 第 2.3 节的菜单路径 | 修正菜单路径 |
| 路由引用统一 | 权限用例"直接路由访问"所用路由须等于 03 环境信息的「前端路由」 | 统一引用环境信息 |

## 6. 文案提取与规范化输出

### 6.1 文案真源链

测试用例中的所有预期文案（校验提示、Toast、错误消息）必须从 02 逐字提取，**禁止改写**：

1. **表单校验文案** ← 02 的"异常处理与已知约束"表第 4 列（提示文案）
2. **Toast 成功文案** ← 02 的操作步骤中"提示 XX 成功"的原文
3. **接口错误 retInfo** ← 02b 的错误码定义表 retInfo 原文
4. **状态标签文案** ← 02 的状态映射表第 1 列（业务名称）

### 6.2 文案对照表

生成测试用例时，同步输出一份《文案对照表》确保逐字一致：

```markdown
## 附录 D. 文案对照表

| 文案用途         | 02 原文                              | 03 用例编号         | 03 中的文案                     | 一致性                          |
| ---------------- | ------------------------------------ | ------------------- | ------------------------------- | ------------------------------- |
| 合同编号格式校验 | 合同编号仅支持字母、数字、-或\_      | FT-FORM-003, ET-FORM-004 | 合同编号仅支持字母、数字、-或\_ | ✅                              |
| 配额行项目必填   | 第 `<行号>` 行检测项目及产品套餐必填 | FT-FORM-009         | 第 X 行检测项目及产品套餐必填   | ✅（`<行号>`→`X` 为占位符替换） |
```

### 6.3 占位符替换规则

02 中的 `<行号>` 等占位符在 03 中可替换为具体示例值（如 `X`、`1`、`2`），但文案其余部分必须逐字一致。替换时在文案对照表中标注。

## 7. 专业术语与通用表述转换对照表

### 7.1 转换原则

- **FT（功能用例）**：面向产品/测试/项目开发管理人，**必须使用通用业务术语**，技术枚举值在括号中注明。
- **IT（接口用例）**：面向开发/联调，**保留技术枚举值**作为请求参数，可在备注中注明业务含义。
- **ET（异常用例）**：混合 — 触发方式用技术值（精确），期望表现用业务术语（可读）。
- **PT（权限用例）**：用业务角色描述，技术权限标识在括号中注明。

### 7.2 状态枚举转换对照表

测试用例中使用的状态值转换规则（以商业化计费配置模块为例，其他模块按 02 状态映射表类推）：

| 02 业务名称 | 02 技术枚举值   | FT 用例中的表述               | IT 用例中的表述               | 备注                                               |
| ----------- | --------------- | ----------------------------- | ----------------------------- | -------------------------------------------------- |
| 正常        | `NORMAL`        | 正常（`NORMAL`）              | `displayStatus=NORMAL`        | 首次提及时用"业务名（`技术值`）"，后续可省略技术值 |
| 即将到期    | `EXPIRING_SOON` | 即将到期（`EXPIRING_SOON`）   | `displayStatus=EXPIRING_SOON` | 同上                                               |
| 已到期      | `EXPIRED`       | 已到期（`EXPIRED`）           | `displayStatus=EXPIRED`       | 同上                                               |
| 已停用      | `DISABLED`      | 已停用（`DISABLED`）          | `status=DISABLED`             | 同上                                               |
| 已终止      | `TERMINATED`    | 已终止（`status=TERMINATED`） | `{status:'TERMINATED'}`       | TERMINATED 非用户可选状态，仅后端只读              |
| 启用中      | `ENABLED`       | 启用中（`ENABLED`）           | `{status:'ENABLED'}`          | 指启停操作的目标状态                               |

### 7.3 数据准备表述

数据准备章节中，状态数据描述**必须使用业务名称**，技术枚举值在括号或备注中注明：

```markdown
# ✅ 正确

3. **合同状态数据**：
   - 1 条 正常（`NORMAL`）合同；
   - 1 条 即将到期（`EXPIRING_SOON`）合同；
   - 1 条 已到期（`EXPIRED`）合同；
   - 1 条 已停用（`DISABLED`）合同。

# ❌ 错误

3. **合同状态数据**：
   - 1 条 `NORMAL` 合同；
   - 1 条 `EXPIRING_SOON` 合同；
```

### 7.4 其他常见术语转换

| 技术表述               | 通用表述                                         | 适用场景       |
| ---------------------- | ------------------------------------------------ | -------------- |
| `retCode=0`            | 接口返回成功                                     | FT/ET 预期结果 |
| `retCode=10022XXX`     | 接口返回错误码 `10022XXX`                        | IT/ET          |
| `Promise.all` 整体失败 | 选项加载与主表加载耦合，任一失败均导致主表不加载 | FT/ET          |
| `collapse-tags`        | 折叠标签展示                                     | FT             |
| `destroy-on-close`     | 关闭时销毁组件实例                               | FT             |
| `readonly = true`      | 只读模式                                         | FT             |
| `hasConsumed=true`     | 已产生分析消耗（`hasConsumed=true`）             | FT 前置条件    |
| `menuId=11013`         | 菜单权限包含 `menuId=11013`                      | PT             |

## 8. 多角色可读性评审机制

### 8.1 评审角色与视角

生成测试用例后，从四个角色视角逐一评审：

| 角色               | 评审重点                                                   | 不通过示例                                                   |
| ------------------ | ---------------------------------------------------------- | ------------------------------------------------------------ |
| **产品**           | 能否看懂用例在测什么业务场景？状态/操作是否用了业务语言？  | "已准备 `NORMAL`、`EXPIRING_SOON` 合同" — 产品不懂这些枚举值 |
| **开发**           | 技术细节是否准确？接口参数、错误码、字段名是否与代码一致？ | "提示 XX 错误" — 未写明 retCode 与 retInfo 原文              |
| **测试**           | 步骤是否可执行？预期是否可验证？是否有歧义？               | "验证状态正确" — 未写明具体期望值                            |
| **项目开发管理人** | 范围是否对齐需求？是否有越界测试或遗漏？优先级是否合理？   | "测试 XX 按钮置灰" — 但 01/02 未定义此行为                   |

### 8.2 评审检查清单

生成完成后逐项核对：

- [ ] 每个 FT 前置条件中的状态值使用业务名称 + 括号技术值
- [ ] 每个 FT 预期结果中的文案与 02 逐字一致
- [ ] 每个 IT 请求参数使用技术字段名与枚举值
- [ ] 每个 ET 期望表现中的 retInfo 与 02b 逐字一致
- [ ] 无推断式置灰/隐藏/禁用（02 未定义的交互行为）
- [ ] 无臆测式校验文案（02 未定义的校验规则）
- [ ] 数据准备章节使用业务名称描述
- [ ] 追踪矩阵覆盖所有 REQ

### 8.3 评审输出

评审结果以警告形式写入 03 文档附录 E（不阻塞产出）：

```markdown
## 附录 E. 可读性评审报告

| 评审角色       | 检查项             | 结果                                          | 备注                       |
| -------------- | ------------------ | --------------------------------------------- | -------------------------- |
| 产品           | 状态值使用业务术语 | ⚠️ FT-LIST-008 前置条件使用 `NORMAL` 等技术值 | 建议改为"正常（`NORMAL`）" |
| 开发           | 接口参数准确       | ✅ 通过                                       | -                          |
| 测试           | 步骤可执行         | ✅ 通过                                       | -                          |
| 项目开发管理人 | 范围对齐           | ✅ 通过                                       | -                          |
```

## 9. 输出格式建议

```markdown
## 03 文档建议结构

- 1. 测试范围（含不测项，对齐 01 边界）
- 2. 测试环境与数据准备
  - 2.1 环境信息（**须含前端 URL、前端路由、前端入口（菜单路径）、后端 URL、测试账号、数据初始化**；路由/菜单取自 `02` 第 2.3 节）
  - 2.2 测试账号矩阵（覆盖 `02` 第 2.3 节全部角色）
  - 2.3 数据初始化要求
- 3. 功能用例 FT-\*（文案逐字匹配 02）
- 4. 接口用例 IT-\*（参数用技术值）
- 5. 权限用例 PT-\*
- 6. 异常用例 ET-{MODULE}-{FEATURE}-\*（retInfo 逐字匹配 02b；分散归入 FT/IT 表格，编号带功能域粒度）
- 7. 回归测试清单
- 附录 A. 需求与测试用例追踪矩阵
- 附录 B. 一致性检查清单
- 附录 C. 上游映射校验报告（可选，NEW）
- 附录 D. 文案对照表（可选，NEW）
- 附录 E. 可读性评审报告（可选，NEW）

> 注：交付模板自带「附录 A. 与下游文档的字段映射」。若启用该附录，追踪矩阵顺延为附录 B、一致性检查清单为附录 C，本技能的 C/D/E 报告依次后移；以实际采用的模板附录编号为基准，避免编号冲突。

## FT 清单

- FT-... -> REQ-... -> {场景} -> {文案来源 02 行号}

## IT 清单

- IT-... -> REQ-... -> {接口} -> {错误码来源 02b 行号}

## 覆盖建议

- UI 自动化：
- API 自动化：
- 手工保留：
```

## 10. 约束

- `FT/IT` 必须沿用对应 `REQ` 的路径编码。
- 不单独维护额外映射文件，直接保持 `REQ -> FT/IT` 主线。
- 要显式区分哪些场景适合 UI 自动化、哪些适合 API 自动化。
- 若 P0 / P1 需求点没有对应测试点，要明确指出。
- **文案禁止改写** — FT 预期结果中的提示文案必须与 02 逐字一致，仅允许占位符（如 `<行号>`）替换为示例值。
- **状态值禁止裸用** — FT 中禁止直接使用技术枚举值描述业务状态，必须用"业务名（`技术值`）"格式。
- **功能禁止越界** — 禁止测试 01/02 未定义的交互行为（如推断式置灰、臆测式校验）。
- **评审必须执行** — 生成完成后必须跑 §8 评审检查清单，结果写入附录 E。
- **路由须对齐详设** — 03 第 2.1 的「前端路由」「前端入口」必须与 `02` 第 2.3 节的路由、菜单路径逐字一致；权限用例"直接路由访问"须引用同一路由值，禁止各自硬编码。
