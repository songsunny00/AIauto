# 02-详细设计文档 生成证据表（模板）

> 本模板由 `detail-design-bootstrap` Skill 生成时实例化填充。
> 与 `02-详细设计文档.md` 一一对应，提供正文每条信息的可追溯证据。
> **不要手工创建此文件** — 由 Skill 运行时自动产出。

---

## 1. 生成元信息

| 字段 | 取值 |
| --- | --- |
| 生成时间 | `{YYYY-MM-DD HH:mm:ss}` |
| Skill 版本 | `detail-design-bootstrap@1.0` |
| 目标模块路径 | `{Prds/Vx.y.z/}` |
| 模板版本 | `Templates/deliverables/02-详细设计文档.md` 的最近修订时间 |
| 输入文件清单 | 见下表 |

**输入文件清单（含 SHA-256 hash）**：

| 输入文件路径 | SHA-256 | 用途 |
| --- | --- | --- |
| `Prds/Vx.y.z/01-需求文档.md` | `{hash}` | REQ 真源 |
| `Prds/Vx.y.z/02b-后台文档.md` | `{hash}` | 接口契约真源 |
| `{前端模块代码根路径}` | `{目录树 hash}` | 实现真源 |
| `Templates/deliverables/02-详细设计文档.md` | `{hash}` | 输出结构基线 |

**生成参数**：

- 模块路径：`{如 D:\Codes\omics-web\src\views\Config\billingConfig}`
- 是否覆盖既有 02：是（用户显式触发）
- 既有 02 备份路径：`Prds/Vx.y.z/02-详细设计文档.md.bak.{YYYYMMDD-HHmmss}`

---

## 2. REQ 来源映射表

| REQ 编号 | 详设章节 | 主要内容来源 | 代码引用 | 备注 |
| --- | --- | --- | --- | --- |
| `REQ-{MODULE}-{FEATURE}-001` | 4.1.4.1 | 需求 + 代码 | `index.vue#L42-58`; `api/modules/billingConfig.ts#L12` | - |
| `REQ-{MODULE}-{FEATURE}-002` | 4.1.4.2 | 需求 + 代码 | `editDialog.vue#L78-120`; `api/modules/billingConfig.ts#L24` | 编辑与明细抽屉共用 `GET /contract/{id}`，按操作上下文拆分 |

> **来源标签说明**：
> - `需求`：内容直接来自 01-需求文档.md
> - `代码`：内容直接来自前端代码（必须附文件路径#行号）
> - `需求 + 代码`：需求有提及，代码实现一致
> - `[DESIGN-SUPPLEMENT]`：需求 + 代码均无，Skill 补充（须在 §4 列出依据）

---

## 3. 需求 ↔ 代码偏差清单

| REQ 编号 | 字段/规则 | 需求版本 | 代码实际版本 | 偏离原因推断 |
| --- | --- | --- | --- | --- |
| `REQ-CONFIG-BILLCFG-FORM-003` | 合同编号校验提示 | `"请输入字母、数字、-或\_"` | `"合同编号仅支持字母、数字、连字符和下划线"` | 文案演进未同步需求（示例，非本次实际数据） |
| `REQ-{MODULE}-{FEATURE}-XXX` | {字段/规则} | {需求版本} | {代码实际版本} | {偏离原因} |

> **正文规则**：详设正文一律写**需求版本**；代码实际版本仅在此清单记录，由人走变更流程同步。
> **历史教训参考**：`FT-BILLCFG-FORM-003/009/010/011` 这类提示文案不一致问题通过本清单自动沉淀。

---

## 4. 逻辑补充清单 [DESIGN-SUPPLEMENT]

| REQ 编号 | 详设章节 | 补充内容 | 补充依据 |
| --- | --- | --- | --- |
| `REQ-{MODULE}-{FEATURE}-XXX` | 4.1.4.X §N | {补充的具体内容摘要} | {如：类似模块 user-config 的实现惯例 / `Prds/AGENTS.md` §4 API 请求规范 / 技术约束} |

> **约束**：
> - 仅当需求 + 代码均无相关实现时才允许补充。
> - 详设正文中必须在补充处加行内标记 `[DESIGN-SUPPLEMENT]`。
> - 补充依据必须可验证（引用文档/模块/技术规范），禁止"凭经验"。

---

## 5. 一致性自检报告

### 5.1 附录 A 14 项检查

| # | 检查项 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | 本文档未重复 `00-全局说明` 已覆盖的全局技术栈、网关、部署、公共安全等内容 | {通过 / 警告 / 失败} | {备注} |
| 2 | 本文档未单独堆砌"系统概述""模块定位""角色分工"等与正文重复的低信息密度章节 | {通过 / 警告 / 失败} | {备注} |
| 3 | 第 2.2 节"实现结构与关键约束"表已包含"对应接口"列 | {通过 / 警告 / 失败} | {备注} |
| 4 | 第 4 章需求实现单元严格继承 `01-需求文档.md` 的 `REQ-...` 编号，一个单元只对应一个需求编号 | {通过 / 警告 / 失败} | {备注} |
| 5 | 第 4 章中的功能名称、业务字段名称、验收编号与 `01-需求文档.md` 保持一致 | {通过 / 警告 / 失败} | {备注} |
| 6 | `###### 3. 输入参数` 表第一列固定为"原字段名"，且所有原字段名均与 01 原文一致 | {通过 / 警告 / 失败} | {备注} |
| 7 | 当业务字段名与前端参数名不一致时，文档中已明确写出字段映射关系 | {通过 / 警告 / 失败} | {备注} |
| 8 | `###### 6. 输出结果` 涉及页面展示时，已使用"业务字段（前端参数名）"写法 | {通过 / 警告 / 失败} | {备注} |
| 9 | 列表、子表、汇总区、明细表、历史表等展示区域，若存在不同字段集，已分区域分别列出输出结果 | {通过 / 警告 / 失败} | {备注} |
| 10 | 若序号、拼接文本、聚合值等由前端生成，文档已明确写出生成口径 | {通过 / 警告 / 失败} | {备注} |
| 11 | 失败场景未写入"输出结果"，统一收口在"异常处理与已知约束" | {通过 / 警告 / 失败} | {备注} |
| 12 | `6.1 实现约束` 已覆盖当前模块的关键落地边界 | {通过 / 警告 / 失败} | {备注} |
| 13 | `6.2 字段映射` 已覆盖核心业务字段的需求字段 → 前端字段 → 接口字段关系 | {通过 / 警告 / 失败} | {备注} |
| 14 | 第 5 章接口清单已覆盖第 4 章"功能动作与接口映射"中出现的全部接口 | {通过 / 警告 / 失败} | {备注} |
| 15 | 全文未出现"待定""TBD""后续补充""暂略"等占位词 | {通过 / 警告 / 失败} | {备注} |
| 16 | 全文未把示例中的业务事实直接复制进正式内容 | {通过 / 警告 / 失败} | {备注} |

> 说明：模板附录 A 原文共 14 条，但因模板有 16 个 checkbox（含第 14、15 两条与本表 #15、#16 对应），此处与模板原顺序对齐列出。

### 5.2 需求 ↔ 代码双向一致性检查

| # | 检查方向 | 检查结果 | 警告项 |
| --- | --- | --- | --- |
| 1 | 需求有 REQ 但代码无任何相关实现 | {通过 / 警告 N 项} | {列出 REQ 编号 + 未实现原因} |
| 2 | 代码有调用但需求未列入该 REQ 的接口清单 | {通过 / 警告 N 项} | {列出接口路径 + 调用处 + 推测归属 REQ} |

### 5.3 退出结论

- 本次自检：{通过 X 项 / 警告 Y 项 / 失败 Z 项}
- 阻塞产出：**否**（软门策略，警告不阻塞）
- 建议后续动作：{如需重生或手工修正的指引}

---

## 6. 关联文件清单（用户已确认）

| 文件路径 | 引用来源 | 用户确认是否纳入 |
| --- | --- | --- |
| `{src/views/Config/billingConfig/index.vue}` | 模块根目录 | ✅ 是 |
| `{src/views/Config/billingConfig/components/editDialog.vue}` | index.vue 引用 | ✅ 是 |
| `{src/api/modules/billingConfig.ts}` | index.vue 引用 | ✅ 是 |
| `{src/composables/useTable.js}` | index.vue 引用 | ✅ 是（仅取相关函数） |
| `{src/components/business/AppForm.vue}` | editDialog.vue 引用 | ✅ 是（仅取 fields/rules 用法） |
| ... | ... | ... |

> 此清单为用户在交互式确认阶段最终裁减后的结果，作为本次生成的代码探查范围证据。
> 后续如需重生，可基于此清单快速复用，但建议重新确认（代码可能已演进）。
