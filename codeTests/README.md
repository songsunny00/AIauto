# codeTests

本目录存放 `code-driven-ui-api-testing`（第 3 种测试方案：读代码验证测试用例，兼顾功能用例与接口用例）过程中产生的过程文档与验证产物。

## 规则
- 只放本 skill 的产物，**不写回** `Tests/` 等其它测试方案的目录（那些脚本不属于本 skill 管辖）。
- **可复用才保留**：接口契约清单、跨用例复用的步骤模板等，沉淀为长期资产。
- **临时文件任务完成后必须删除**：单次运行的探针脚本、curl 日志、中间草稿等，验证任务结束即清理，不残留。
