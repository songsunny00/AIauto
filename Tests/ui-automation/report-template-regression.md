# {{项目版本}} {{模块名称}} - UI 自动化回归测试报告

> **报告路径**: `{{报告路径}}`
> **执行日期**: {{YYYY-MM-DD}}
> **执行方式**: playwright-test（确定性回归脚本，非探索式）
> **测试范围**: {{一级模块}} / {{二级模块}}（或全量）
> **用例来源**: `03-测试用例文档.md`（编号 `ATS-*`）
> **报告生成**: {{AI 自动生成 / 机械统计 + AI 总结}}

---

## 一、测试概述

### 1.1 测试对象

| 项 | 内容 |
| --- | --- |
| 模块 | {{模块名称}} |
| 前端入口 | `{{页面URL}}` |
| 鉴权方式 | `storageState` 复用登录态（global-setup 生成，免重复登录） |
| 测试账号 | `{{账号}}`（{{角色信息}}） |
| 定位策略 | `data-testid` > `getByRole/name` > 稳定属性 > CSS/text 兜底 |

### 1.2 结果总览

| 维度 | 数量 | 占比 |
| --- | --- | --- |
| 用例总数 | {{总数}} | 100% |
| ✅ 通过 | {{数量}} | {{百分比}}% |
| ❌ 失败 | {{数量}} | {{百分比}}% |
| ⏭️ 跳过 | {{数量}} | {{百分比}}% |
| ⚠️ 错误 | {{数量}} | {{百分比}}% |
| **已执行用例** | **{{数量}}** | **{{百分比}}%** |

### 1.3 关键指标

| 指标 | 公式 | 结果 | 说明 |
| --- | --- | --- | --- |
| **通过率** | 通过 / 已执行 | **{{百分比}}%**（{{通过}}/{{已执行}}） | — |
| **失败率** | 失败 / 已执行 | **{{百分比}}%**（{{失败}}/{{已执行}}） | — |
| **跳过率** | 跳过 / 总用例 | **{{百分比}}%**（{{跳过}}/{{总数}}） | 基于实际数据检查的 `test.skip` |
| **总耗时** | Σ 各用例 | **{{耗时}}s** | 含登录态复用，不含首次 global-setup |

---

## 二、技术方案（简要）

| 层次 | 说明 |
| --- | --- |
| 测试框架 | `@playwright/test` `{{pwVersion}}` |
| 浏览器 | {{浏览器}}（由 `PW_BROWSER_CHANNEL` 决定） |
| 登录态 | `global-setup.ts` 生成 `storageState`，各 fixture 复用 |
| 定位优先级 | `data-testid` > `getByRole/name` > 稳定属性 > 兜底 CSS/text |
| 断言原则 | 优先验证真实业务结果；文案已明确时完整文案精确匹配 |
| 产物 | `junit.xml` + HTML + 失败截图/视频/`trace.zip`（retain-on-failure） |

---

## 三、用例明细

> 按 spec 文件 / 用例文档章节分组；证据列附失败用例的截图与 trace 相对路径。

### 3.1 {{分组1名称}}

| # | 用例编号 | 结果 | 耗时(s) | 证据 / 产物 |
| --- | --- | --- | --- | --- |
| 1 | {{ATS-编号}} | ✅ 通过 | {{t}} | — |
| 2 | {{ATS-编号}} | ❌ 失败 | {{t}} | 截图 `{{path}}` / trace `{{path}}`（详见备注 #{{N}}） |
| 3 | {{ATS-编号}} | ⏭️ 跳过 | — | {{skip 原因}} |

### 3.2 {{分组2名称}}

| # | 用例编号 | 结果 | 耗时(s) | 证据 / 产物 |
| --- | --- | --- | --- | --- |
| {{N}} | {{ATS-编号}} | ✅ 通过 | {{t}} | — |

---

## 四、非通过用例备注（AI 归因）

> 由 AI 基于 `junit.xml` 失败信息与失败工件（截图 / `trace.zip` / `error-context.md`）逐条归因，根因**只归为以下五类之一**：
> `ENV`（环境/配置）· `DATA`（测试数据/前置条件）· `SCRIPT`（脚本/定位/断言）· `DEFECT`（真实功能缺陷）· `CHANGE`（需求/页面变更致脚本失效）。

### ❌ 失败（{{N}} 项）

#### 备注 #1：{{ATS-编号}} — {{简短描述}}

| 项 | 内容 |
| --- | --- |
| **用例预期** | {{预期行为/文案}} |
| **实际结果** | {{实际行为/文案}} |
| **测试操作** | {{关键步骤}} |
| **失败原因分类** | {{ENV / DATA / SCRIPT / DEFECT / CHANGE}} |
| **DOM 状态** | {{失败时刻关键 DOM / 提示信息}} |
| **功能是否生效** | {{是/否（说明）}} |
| **修复建议** | {{脚本侧修正 / 推前端 / 推后端}} |
| **优先级** | P{{0/1}} |
| **失败截图** | `{{相对路径}}` |
| **trace** | `{{相对路径}}` |

### ⏭️ 跳过（{{N}} 项）

#### 备注 #{{N}}：{{ATS-编号}} — {{简短描述}}

| 项 | 内容 |
| --- | --- |
| **跳过原因** | {{基于实际数据检查得出的 skip 原因}} |
| **已验证部分** | {{通过其他用例间接验证的部分}} |
| **未验证部分** | {{未覆盖部分}} |
| **建议** | {{补测方式}} |
| **优先级** | P{{0/1}} |

---

## 五、缺陷汇总

### 5.1 确认缺陷（{{N}} 项）

| 缺陷编号 | 关联用例 | 严重程度 | 缺陷描述 | 修复建议 |
| --- | --- | --- | --- | --- |
| BUG-001 | {{ATS-编号}} | {{高/中/低}} | {{描述}} | {{建议}} |

### 5.2 待确认问题（{{N}} 项）

| 问题编号 | 关联用例 | 问题描述 | 需确认内容 |
| --- | --- | --- | --- |
| ISSUE-001 | {{ATS-编号}} | {{描述}} | {{确认内容}} |

### 5.3 脚本/定位待优化（{{N}} 项）

| 编号 | 关联用例 | 问题 | 优化建议 |
| --- | --- | --- | --- |
| OPT-001 | {{ATS-编号}} | {{脆弱定位/过宽断言}} | {{改用 data-testid / 完整文案断言}} |

---

## 六、AI 执行总结

{{由 AI 基于以下信息生成的总结段落：
- 本次覆盖的用例数、通过/失败/跳过分布与覆盖度；
- 失败用例的主要根因聚类（ENV/DATA/SCRIPT/DEFECT/CHANGE 各自占比与典型表现）；
- 核心功能是否可用、最需关注的高优先级失败；
- 是否发现真实功能缺陷（DEFECT），相对上一轮是否有回归/改善；
- 一句话总体结论。}}

---

## 七、结论与建议

### 7.1 测试结论

{{总结：核心功能是否正常、是否达到可发布/可回归门禁标准。}}

### 7.2 改进建议

1. **{{建议类别1}}**：{{具体建议}}
2. **{{建议类别2}}**：{{具体建议}}
3. **{{建议类别3}}**：{{具体建议}}

---

*报告生成时间：{{YYYY-MM-DD}}*
*测试工具：playwright-test（Playwright @playwright/test）*
*测试环境：{{环境URL}}*
*结构化数据源：`execution-digest.json` + `junit.xml` + 失败工件*
